function Login02(){
    const isConected = true

    return (
        <div>
            O usuário, UserName, está {isConected ? 'CONECTADO' : 'DESCONECTADO'}.
        </div>
    )
}

export default Login02